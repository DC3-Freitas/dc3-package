"""
INCOMPLETE
h5 dataset file generation for torch/ML
"""

from lattice.gen import LatticeGenerator
from features.compute_all import compute_feature_vectors
import numpy as np
import h5py

def create_h5(lattice_generator, alpha_min, alpha_max, num_samples, save_path):
    """
    Creates an h5 file with num_samples of synthetic data generated by lattice_generator.
    The data is saved to save_path.

    Args:
        lattice_generator (LatticeGenerator): A LatticeGenerator object used to generate synthetic data.
        num_samples (int): The number of samples to generate.
        save_path (str): The path to save the h5 file.
    """
    # Create a new h5 file
    with h5py.File(save_path, "w") as f:
        # Create a dataset for the features
        features = f.create_dataset("features", (num_samples, 2), dtype="f")
        # Create a dataset for the labels
        labels = f.create_dataset("labels", (num_samples,), dtype="i")

        # Load generated dataset